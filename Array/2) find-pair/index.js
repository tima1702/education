/*
  Задача
  Найти в массиве пару повтаряющихся чисел, если таковой нет, вернуть null
  Примечание
  В функцию всегда будет передан массив числе, и максимум только одна пара
 */

export default function findPair(arr) {
  // код сюда
}
