/*
  Задача:
  Написать функцию сумирования, в которую передается два аругмента (числа) и должна возвращаться их сумма.
  Замечания:
  Параметра будет всегда два, и это будут всегда числа
 */

function sum(a, b) {
  // Писать код сюда
}

export default sum;
