/*
  Задача
  Написать функцию calc, которая принемает три аргумента:
  1 - Операции: '+', '-', '/', '*'
  2, 3 - Числа
  Примеры:
   calc('+', 2, 3) === 5
   calc('*', 2, 3) === 6
   calc('-', 10, 5) === 5
   calc('/', 10, 5) === 2
   Необходимо использовать switch
   Примечание: Проверку аргументов делать не нужно
 */

export default function basicCalc(operation, a, b) {
  // код сюда
}
